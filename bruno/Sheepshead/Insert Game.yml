info:
  name: Insert Game
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_url}}/games/1"
  auth:
    type: apikey
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
